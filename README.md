# trdQueue

Low-latency market-data pipeline and order-book engine. This repository is a portfolio project: the C++ core is a pure hot path, and everything that talks to the network, disk, or a browser lives outside it.